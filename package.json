{
  "name": "jmtfpvg",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "wrangler": "^3.114.1"
  }
}
